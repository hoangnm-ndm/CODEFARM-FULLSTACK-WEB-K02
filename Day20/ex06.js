console.log(Math.floor(Math.random() * 10));

const students = ["Vinh", "Hoang", "Thuan", "Van Anh"];

function randomName(list) {}

/**
 * * Xây dựng 1 hàm nhận vào một mảng và trả về 1 phần tử ngẫu nhiên của mảng.
 */
