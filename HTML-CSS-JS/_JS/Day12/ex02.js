/**
 * * for
 * * for..in
 * * for..of
 * * while
 * * do while
 */

let i = 0;
for (;;) {
	if (i < 1) {
		console.log(i);
	}
	i++;
}

/**
 * * Bước 1: Khởi tạo biến đếm i (chỉ làm 1 lần)
 * * Bước 2: Kiểm tra điều kiện
 * * Bước 3: Nếu điều kiện đúng, thực hiện code block. Nếu sai, dừng.
 * * Bước 4: Thực hiện bước nhày, sau đó quay lại bước 2.
 *
 */

function printSquare(n) {
	// * in ra hình vuông bằng dấu (*)
	// * Với n là độ dài cạnh của hình vuông
}
