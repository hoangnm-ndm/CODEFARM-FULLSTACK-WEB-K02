function reverseArr(arr) {
	for (let i = 0; i < arr.length; i++) {
		console.log(arr[i]);
	}
}

/**
 * * Đảo ngược mảng đưa vào nhưng không dùng phương thức reverse mà chỉ dùng vòng lặp for và các phương thức vừa được học.
 */

reverseArr([1, 2, 3]); // [3,2,1]
